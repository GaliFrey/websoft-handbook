# История изменений

## 11.09.2026 13:08 — Редакторская проверка статьи 906_PSQL

- Статья целиком проверена по стендовому отчёту и соседним материалам.
  Внутренние методические сведения, endpoint, DLL, ID кейсов, команды
  проверки, статистика прогона, учётные данные и сведения о тестовой базе в
  публичном тексте отсутствуют.
- Уточнены формулировки о псевдонимах PostgreSQL, поддержке явных соединений,
  `ForeignElem()` и двух формах `ForeignDispName`; поведение, XQuery, SQL и
  публичная структура статьи не изменены.
- Проверки: все 45 SQL-блоков совпадают с отчётом стенда после нормализации
  пробелов и регистра; строгая сборка, локальные ссылки, границы блоков кода,
  проверка на служебные маркеры и `git diff --check` прошли. Визуальная
  проверка не выполнялась; dev-сервер и стенд не запускались, не
  перезапускались и не останавливались.

## 11.09.2026 13:00 — Статья XQuery для 906_PSQL

- Добавлена статья `906 + PostgreSQL` по канонической матрице из 61 XQuery,
  подключена в обзор раздела и навигацию сайта. Зафиксированы WebSoft HCM
  Server `2023.2.906`, PostgreSQL provider `1.24.4.18` и PostgreSQL `16.3`.
- Полный прогон через `XQueryInspector 1.2.1` дал 53 ожидаемых результата,
  шесть ошибок provider и два подтверждённых опасных поведения. 59 запросов
  преобразованы в SQL; прямой `EXPLAIN` прошли 56 основных SQL, 57
  `countSql` выполнились.
- Подтверждено отличие от `434_PSQL`: заработали псевдонимы полей, явные
  `join`, `ljoin`, `rjoin` и антисоединение через `ljoin` с `MatchSome()`.
  `ForeignElem()` формирует `LEFT JOIN`; полный каталог `collaborators`
  содержит 55 полей; в иерархический CTE добавлено `kpi_profiles_id`.
- Две формы `not MatchSome()`, обе формы `ForeignDispName` и однострочная
  запись с обычным условием перед `IsHierChild()` сохранили ошибки. Два
  запроса без `order by Hier()` по-прежнему теряют иерархический фильтр.
  Точная многострочная форма с обычным условием перед `IsHierChild()`
  сформировала рабочий CTE.
- Для `distinct()` подтверждена поздняя ошибка `42P10` из-за добавляемого
  provider `order by id`; вариант с явным `order by fullname` выполнился.
  Внутренний отчёт сохранён без учётных данных и cookie.
- Проверки: все 45 SQL-блоков статьи совпали с отчётом стенда после
  нормализации пробелов и регистра; структура отчёта, границы блоков кода,
  локальные ссылки, `git diff --check` и строгая сборка прошли. Визуальная
  проверка не выполнялась; dev-сервер и стенд не запускались, не
  перезапускались и не останавливались.

## 11.09.2026 12:29 — Статья XQuery для 906_MSSQL

- Добавлена статья `906 + MSSQL` по той же матрице из 61 XQuery, подключена в
  обзор раздела и навигацию сайта. Зафиксированы WebSoft HCM Server
  `2023.2.906`, MSSQL provider `1.24.4.18` и Microsoft SQL Server
  `16.0.4275.2`.
- Полный прогон через `XQueryInspector 1.2.1` дал 53 ожидаемых результата,
  шесть ошибок provider и два подтверждённых опасных поведения. Из 61 запроса
  59 преобразованы в SQL; прямую компиляцию прошли 55 основных SQL, 56
  `countSql` выполнились.
- Подтверждено отличие от 434_MSSQL: явные `join`, `ljoin`, `rjoin` и
  антисоединение через `ljoin` с `MatchSome()` теперь работают.
  `ForeignElem()` формирует `LEFT JOIN`; полный каталог `collaborators`
  содержит 55 полей; в иерархический CTE добавлено `kpi_profiles_id`.
- Три отрицания `MatchSome()`, обе формы `ForeignDispName` и однострочная
  запись с обычным условием перед `IsHierChild()` сохранили прежние ошибки.
  Два запроса без `order by Hier()` по-прежнему теряют иерархический фильтр.
- Для запроса с обычным условием перед `IsHierChild()` показана зависимость
  от форматирования: однострочная форма завершилась ошибкой, точная
  многострочная сформировала рабочий CTE. Для переносимости между 434 и 906
  рекомендована однострочная форма с `IsHierChild()` первым условием.
- Для проверки обычного `doc-contains()` на тестовом MSSQL создан и заполнен
  полнотекстовый индекс `dbo.collaborator(data)`; запрос после этого выполнен
  через endpoint и прямой `countSql`.
- Публичный текст очищен от методических заметок и сведений о тестовой базе.
  Проверки: 44 полных SQL-блока сопоставлены с отчётом стенда, сокращённый
  `return $elem` проверен по составу из 55 полей, сортировка выполнена
  отдельно; строгая сборка, локальные ссылки и `git diff --check` прошли.
  Визуальная проверка не выполнялась; dev-сервер сайта не запускался.

## 11.09.2026 00:44 — Согласование статей 434_MSSQL и 434_PSQL

- Обе статьи приведены к единой структуре и уровню подробности: одинаковые
  разделы, порядок примеров, 51 блок XQuery и 41 блок SQL.
- Из публичного текста удалены методика инспекции, метрики прогонов,
  `EXPLAIN`, `countSql`, журналы provider, коды ошибок и подробности тестовых
  данных. Сохранены только XQuery, сформированный SQL, параметры, смысл
  преобразования и ограничения, влияющие на прикладной код.
- Из PostgreSQL-статьи удалён поздний `order by id`, поскольку его не
  возвращает преобразование XQuery. Добавлены полные пары XQuery/SQL для
  отрицаний `null()`, пустой строки, `IsEmpty()` и трёх вариантов кастомного
  `doc-contains()`.
- В MSSQL-статью добавлены полные запросы двух форм `not MatchSome()`,
  упрощено описание полнотекстового поиска и синхронизированы предупреждения
  об однострочной форме иерархических запросов.
- Проверки: заголовки и число блоков обеих статей совпадают; 41 SQL-блок
  PostgreSQL-статьи совпадает по токенам со стендовым отчётом; строгая сборка
  и `git diff --check` прошли. Визуальная проверка не выполнялась;
  dev-сервер и стенд не перезапускались.

## 11.09.2026 00:21 — Предупреждение о переносах в Hier-запросах

- В начале раздела иерархии статьи `434 + PostgreSQL` добавлено отдельное
  предупреждение: XQuery с `IsHierChild()` или `IsHierChildOrSelf()` вместе с
  `Hier()` нужно передавать в точной однострочной форме.
- Указана наблюдаемая причина: строковый legacy-препроцессор чувствителен к
  пробелам и переносам; разрыв между `)` и `and` оставляет лишние `where` и
  `and` в effective XQuery.
- Ограничение явно отделено от обычных XQuery без иерархической обработки.
- Проверки: строгая сборка и `git diff --check` прошли. Визуальная проверка не
  выполнялась; dev-сервер и стенд не перезапускались.

## 11.09.2026 00:18 — Точная форма иерархических XQuery

- По присланному результату выявлена чувствительность строковой подготовки
  иерархии к переносам: разрыв между `)` и `and` оставляет в
  `effectiveXQuery` лишние `where` и `and`.
- Сравнение со стендовым отчётом подтвердило, что SQL, `countSql` и параметры
  при этом совпадают; различается только effective XQuery.
- Все иерархические примеры статьи переведены в точную однострочную форму
  канонической матрицы, включая опасные варианты и запросы с дополнительным
  условием. В предупреждение добавлено явное ограничение на форматирование.
- Проверки: присланный и канонический JSON сопоставлены по отдельным полям;
  точная форма рабочего запроса повторно проверена на `434_PSQL`; строгая
  сборка и `git diff --check` прошли. Визуальная проверка не выполнялась;
  dev-сервер и стенд не перезапускались.

## 11.09.2026 00:10 — Полные иерархические запросы в предупреждении

- В предупреждении о порядке условий `IsHierChild()` сокращённые фрагменты
  заменены полными XQuery с `for`, `where`, `order by` и `return`.
- Для обратного порядка добавлены полный исходный запрос и полный невалидный
  effective XQuery, в котором препроцессор удаляет `where` и оставляет лишний
  `and`.
- Причина: сделать оба сценария воспроизводимыми без восстановления
  отсутствующих частей запроса читателем.
- Проверки: запросы сверены с канонической матрицей и отчётом инспектора;
  строгая сборка и `git diff --check` прошли. Визуальная проверка не
  выполнялась; dev-сервер и стенд не перезапускались.

## 11.09.2026 00:01 — Повторный прогон с инспектором 1.2.1

- Все 61 канонический XQuery повторно выполнены на `434_PSQL` с
  `XQueryInspector 1.2.1+a5d448b2b7b1db7b27110e2f10aee033a17c9d72`;
  внутренний отчёт полностью заменён результатами нового прогона.
- Типы параметров в статье синхронизированы с новой нормализацией инспектора:
  `bigint`, `varchar`, `timestamp` и `varchar[]`. Для множественного
  `MatchSome()` удалено прежнее числовое значение типа.
- SQL и effective XQuery не изменились; обновился только случайный служебный
  псевдоним `ForeignElem()`. Тестовые данные изменились: множественный
  `MatchSome()` теперь возвращает две строки, трёхтабличный `some/satisfies` —
  одну; `expectedHasRows` матрицы актуализирован.
- Итог: 55 из 61 инспекций успешны, 50 из 55 основных SQL прошли `EXPLAIN`,
  53 из 55 `countSql` выполнены; классификация — 47 ожидаемых результатов,
  12 ошибок и 2 опасных поведения.
- Проверки: все 36 SQL-блоков статьи совпадают по токенам с новым отчётом,
  матрица содержит 61 уникальный кейс, строгая сборка и `git diff --check`
  прошли. Визуальная проверка не выполнялась; dev-сервер и стенд не
  перезапускались.

## 10.09.2026 23:44 — Полные XQuery для отрицания MatchSome()

- В статье `434 + PostgreSQL` сокращённые условия `not MatchSome(...)` и
  `not(MatchSome(...))` заменены полными каноническими запросами с `for`,
  `where` и `return`.
- Причина: показывать рядом с полным сформированным SQL воспроизводимый
  исходный XQuery, а не отдельный фрагмент условия.
- Проверки: оба XQuery совпадают с определениями матрицы, строгая сборка и
  `git diff --check` прошли. Визуальная проверка не выполнялась; dev-сервер и
  стенд не перезапускались.

## 10.09.2026 23:41 — Тип массива в параметре PostgreSQL

- В описании множественного `MatchSome()` расшифрован тип `-2147483626` как
  `NpgsqlDbType.Array | NpgsqlDbType.Varchar`.
- Уточнено, что Npgsql связывает `@p0` как типизированный параметр, а не
  подставляет JSON в SQL. Для ручного выполнения приведён эквивалент
  `ARRAY['123', '456']::varchar[]`; тип массива должен совпадать с типом
  конкретной колонки.
- Причина: отделить представление значения в инспекторе от SQL-литерала,
  необходимого при прямой проверке команды.
- Проверки: тип `category_id` и выполнение параметра `varchar[]` ранее
  подтверждены в WTDB; строгая сборка и `git diff --check` прошли.
  Визуальная проверка не выполнялась; dev-сервер и стенд не
  перезапускались.

## 10.09.2026 23:27 — Актуальное описание множественного MatchSome()

- Из статьи `434 + PostgreSQL` удалён оставшийся разбор прежнего числового
  варианта `match-some-multiple-field`.
- Описание теперь относится только к каноническому запросу с кодами `123` и
  `456`: один параметр-массив, оператор пересечения `&&`, успешное выполнение
  SQL и пустой результат из-за незаполненных `category_id` на стенде.
- Причина: не смешивать текущий канонический пример с заменённым тестовым
  запросом.
- Проверки: старые числовые значения отсутствуют в статье и внутреннем
  описании стенда; строгая сборка и `git diff --check` прошли. Визуальная
  проверка не выполнялась; dev-сервер и стенд не перезапускались.

## 10.09.2026 23:14 — Текстовые коды в множественном MatchSome()

- Канонический `match-some-multiple-field` переведён с числовых ID на
  текстовые коды `'123'` и `'456'`; матрица и обе статьи синхронизированы.
- Для `434_PSQL` зафиксирован точный SQL без `::text[]` и параметр
  `p0` типа `-2147483626` со значением `["123","456"]`. Запрос и `countSql`
  выполняются с параметром `varchar[]`; результат пустой из-за отсутствия
  значений `category_id` в тестовых данных.
- Числовая форма оставлена в PostgreSQL-статье как отдельный нерабочий
  вариант: она создаёт несовместимое условие `varchar[] && text[]`.
- Итог PostgreSQL-проверки обновлён: 50 из 55 основных SQL прошли `EXPLAIN`,
  53 из 55 `countSql` выполнены. MSSQL после изменения кейса не
  инспектировался; необходимость повтора отмечена в статье и внутреннем
  отчёте.
- Проверки: обновлённый XQuery выполнен через `tools.xquery()`, SQL проверен в
  WTDB с параметром `varchar[]`, 36 SQL-блоков статьи совпадают по токенам с
  отчётом инспектора, матрица содержит 61 уникальный кейс; строгая сборка и
  `git diff --check` прошли. Визуальная проверка не выполнялась; dev-сервер и
  стенд не перезапускались.

## 10.09.2026 23:04 — Соединение каталогов без псевдонима

- Из канонического кейса `join-catalog-list` удалён псевдоним
  `position_name`: раздел о псевдонимах уже показывает их поведение, поэтому
  пример соединения теперь проверяет только перечисление каталогов.
- XQuery и SQL синхронизированы в статьях `434 + PostgreSQL` и
  `434 + MSSQL`. Для PostgreSQL обновлены стендовый отчёт и итог проверки:
  основной SQL прошёл `EXPLAIN`, запрос вернул одну строку, число успешных
  основных SQL выросло с 48 до 49.
- MSSQL после изменения канонического XQuery не перезапускался. В статье и
  внутреннем отчёте явно отмечена необходимость повторной инспекции; прежний
  отчёт сохранён как фактический результат варианта с псевдонимом.
- Проверки: в матрице осталось 61 уникальное определение; PostgreSQL SQL
  выполнен через `tools.xquery()` и проверен напрямую в WTDB; 36 SQL-блоков
  PostgreSQL-статьи совпадают по токенам с отчётом инспектора; строгая сборка
  и `git diff --check` прошли. Визуальная проверка не выполнялась; dev-сервер
  и стенд не перезапускались.

## 10.09.2026 22:52 — Точный SQL инспектора в статье 434_PSQL

- Все 36 SQL-блоков статьи `434 + PostgreSQL`, соответствующие результатам
  инспектора, сверены с сохранённым отчётом и приведены без упрощения
  выражений: восстановлены внешние и внутренние скобки, полные условия и
  полные рекурсивные CTE.
- Сокращённые SQL для `IsHierChildOrSelf()` и `CatalogHierSubset()` заменены
  полными командами. Для двух нерабочих форм `not MatchSome()` вместо
  отдельных условий показаны полные SQL.
- В начале статьи явно закреплено правило: допускаются только переносы строк
  и отступы; скобки, кавычки и операторы инспектора сохраняются.
- Причина: статья должна показывать фактически сформированный SQL, а не его
  семантически эквивалентную редакторскую нормализацию.
- Проверки: токены 36 блоков программно сопоставлены с SQL из отчёта
  `434_PSQL`; строгая сборка документации и `git diff --check` прошли.
  Визуальная проверка не выполнялась; dev-сервер и стенд не
  перезапускались.

## 10.09.2026 22:44 — Экранирование `_` в PostgreSQL contains()

- В статье `434 + PostgreSQL` исправлено значение параметра для
  `contains(..., '_')`: фактическая строка `%\_%` содержит одну обратную
  косую черту, а удвоение наблюдалось только в JSON-представлении.
- Уточнён вывод: обратная косая черта экранирует `_` для PostgreSQL
  `LIKE`/`ILIKE`, поэтому символ ищется как литерал.
- Проверки: строка параметра содержит четыре символа с кодами
  `37, 92, 95, 37`; строгая сборка документации и `git diff --check` прошли.
  Визуальная проверка не выполнялась; dev-сервер и стенд не
  перезапускались.

## 10.09.2026 21:40 — Статья о XQuery для 434 и PostgreSQL

- Добавлена статья `434 + PostgreSQL` по общей матрице из 61 XQuery: поля,
  условия, каталоги, `MatchSome()`, `ForeignElem()`, `ForeignDispName()`,
  `doc-contains()`, иерархия, `distinct()` и сортировка. Страница включена в
  обзор раздела и навигацию.
- Зафиксированы отличия PostgreSQL provider: `ILIKE`, `XMLExists()`, массивы
  и `WITH RECURSIVE`, а также невалидные псевдонимы, числовой множественный
  `MatchSome()`, формы `not`, `ForeignDispName()` и поздний `order by id` у
  `distinct()`. Для применимых случаев приведены проверенные обходы.
- На стенде `434_PSQL` отключены `LuceneFTIndex` и `XQueryTemplateCache`.
  Полный результат сохранён во внутреннем отчёте
  `resources/drafts/xquery-to-sql/stand-results/434-psql.json`.
- Проверки: 61 XQuery выполнен на WebSoft HCM 434 с PostgreSQL 16.3;
  55 инспекций успешны, 48 из 55 основных SQL прошли `EXPLAIN`, 52 из 55
  `countSql` выполнены. Строгая сборка, ссылки новой страницы, подписи блоков
  XQuery/SQL и `git diff --check` проверены. Визуальная проверка не
  выполнялась; dev-сервер не запускался, не перезапускался и не
  останавливался. Стенд WebSoft перезапущен для загрузки DLL и применения
  настроек; PostgreSQL не перезапускался.

## 10.09.2026 19:09 — Единые подписи блоков XQuery и SQL

- Перед всеми 49 блоками XQuery и 40 блоками SQL в статье `434_MSSQL`
  добавлены явные подписи `XQuery:` и `SQL:`.
- Отдельные подписи «Итоговый SQL» у `IsHierChild()` и
  `IsHierChildOrSelf()` удалены; основные и вложенные примеры теперь оформлены
  одинаково.
- Причина: визуально различать исходный запрос и сформированный SQL без
  исключений в отдельных разделах.
- Проверки: количество подписей совпадает с количеством соответствующих
  блоков кода, строгая сборка документации и `git diff --check`. Визуальная
  проверка не выполнялась; dev-сервер не запускался и не перезапускался.

## 10.09.2026 19:00 — Матрица XQuery перенесена к статье

- Канонический набор из 61 XQuery перенесён из внутренних материалов в
  `docs/explanations/xquery-to-sql/files/cases.json` и добавлен как скачиваемый
  файл на обзорную страницу раздела.
- Каталог `resources/` снова полностью исключён из Git. Серверный шаблон,
  подробные результаты стендов, пробный отчёт и несвязанные черновики остались
  локальными и больше не появляются среди файлов для коммита.
- Из корневого `README.md` удалено описание отслеживаемого
  `resources/drafts/`, поскольку это больше не соответствует структуре
  репозитория.
- Причина: хранить публикуемую матрицу рядом со статьями и не включать в Git
  внутренние инструменты и результаты подготовки.
- Проверки: JSON содержит 61 кейс с уникальными ID; ссылка на скачиваемый файл,
  строгая сборка документации и `git diff --check` проверены. Визуальная
  проверка не выполнялась; dev-сервер не запускался и не перезапускался.

## 10.09.2026 18:50 — Удаление служебного происхождения SQL из статьи

- Из статьи `434_MSSQL` удалены упоминания журнала Mode Debug, прямого вызова
  provider и форматирования SQL для чтения. Иерархические запросы теперь, как
  и остальные примеры, сопровождаются нейтральной подписью «Итоговый SQL».
- Служебные формулировки в разделах неподдерживаемых соединений,
  `MatchSome()`, `ForeignDispName()` и иерархии заменены описанием результата:
  сформированного SQL, ошибки или сохранённого условия.
- Причина: способ получения SQL относится к внутренней подготовке материала и
  не нужен читателю статьи.
- Проверки: отсутствие служебных формулировок в статье, строгая сборка
  документации и `git diff --check`. Визуальная проверка не выполнялась;
  dev-сервер не запускался и не перезапускался.

## 10.09.2026 18:20 — Удаление демо-инструмента из публичной статьи

- Из публикуемой статьи `434_MSSQL` удалены упоминания служебного инструмента
  получения SQL. Источник примеров теперь описан через прямую трансляцию
  provider, журнал Mode Debug и проверку сформированных команд в MSSQL.
- Формулировки про параметры множественного `MatchSome()`, отрицание
  `MatchSome()` и успешное выполнение иерархических запросов переписаны без
  зависимости от демо-функции.
- Внутренние результаты стенда и служебные материалы не изменялись: они не
  публикуются и сохраняют технические сведения для воспроизводимости.
- Причина: демо-функция не является частью описываемого поведения WebSoft HCM
  и не должна фигурировать в пользовательской статье.
- Проверки: отсутствие упоминаний в `docs/`, строгая сборка документации и
  `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не
  запускался и не перезапускался.

## 10.09.2026 18:11 — Однозначное описание неподдерживаемых join

- Раздел статьи `434_MSSQL` переименован в «Неподдерживаемые join, ljoin и
  rjoin». Явно указано, что все четыре примера, включая антисоединение через
  `ljoin` и `MatchSome()`, завершаются ошибкой до формирования SQL.
- Уточнено, что в антисоединении разбор останавливается на неподдерживаемом
  `ljoin`, поэтому `MatchSome()` не является причиной этой ошибки.
- Рабочие соединения через перечисление каталогов, `some/satisfies` и
  `ForeignElem()` отделены от явных операторов: это другой синтаксис XQuery,
  который provider самостоятельно преобразует в SQL `JOIN`.
- Причина: исключить ошибочное прочтение, будто `join/ljoin/rjoin` работают на
  сборке 434.
- Проверки: строгая сборка документации и `git diff --check`. Визуальная
  проверка не выполнялась; dev-сервер не запускался и не перезапускался.

## 10.09.2026 17:49 — Расширение матрицы XQuery для 434_MSSQL

- Каноническая матрица расширена с 46 до 61 кейса: добавлены все числовые
  операторы сравнения, диапазон дат, возврат всей записи, специальные символы
  `contains()`, множественное поле в `MatchSome()`, `ForeignDispName` и два
  порядка дополнительного условия с `IsHierChild()`. Отдельный тест пустого
  результата и повторная проверка `Child/ChildOrSelf` не добавлялись.
- Подтверждено, что `return $elem` разворачивается в явный список из 48 полей,
  а даты преобразуются через `convert(varchar(19), ..., 127)`.
- Для множественного `collaborators.category_id` функция `MatchSome()`
  формирует XML-метод `.exist(...)`; SQL успешно выполнен в MSSQL.
- Уточнено экранирование `contains()`: `_` превращается в `[_]`, `%` остаётся
  маской, `[` передаётся без дополнительного экранирования.
- Выявлена зависимость иерархического препроцессинга от порядка условий:
  `IsHierChild(...) and условие` работает, а `условие and IsHierChild(...)`
  формирует XQuery без `where` и завершается ошибкой около `and`.
- Проверены документированная атрибутная и функциональная формы
  `ForeignDispName`. В условии атрибутная форма не транслируется, в сортировке
  создаётся невалидное обращение к `BigInt`, а функциональная форма требует
  отсутствующую в WTDB SQL-функцию. В статье рекомендованы `ForeignElem()` или
  соединение каталогов.
- Кейс `distinct()` переведён на понятное поле `collaborators.fullname`;
  существующая сортировка уже выполняется по этому же полю.
- Полный прогон: 61 XQuery, 55 успешных инспекций и выполнений через endpoint;
  51 из 55 основных SQL и 52 из 55 `countSql` прошли проверку в WTDB. Итоговая
  классификация: 49 успешных, 10 ошибок и 2 опасных поведения.
- Причина: дополнить эталонную статью конструкциями, которые меняют форму SQL
  или обнаруживают несовместимость provider, без превращения её в журнал
  внутреннего тестирования.
- Проверки: полный прогон через серверный endpoint, отдельная компиляция и
  выполнение SQL в MSSQL, согласованность 61 определения с отчётом. Визуальная
  проверка не выполнялась; dev-сервер и стенд не запускались, не
  перезапускались и не останавливались.

## 10.09.2026 17:14 — Потеря иерархического фильтра без Hier()

- В статье `434_MSSQL` добавлено опасное поведение `tools.xquery()`:
  `IsHierChild()` и `IsHierChildOrSelf()` без `order by $elem/Hier()` удаляются
  legacy-препроцессором без эквивалентной замены, после чего запрос выбирает
  весь каталог `subdivisions`.
- Показаны исходный XQuery, фактически переданный provider запрос без условия
  и итоговый SQL без `WHERE`. Уточнено, что `success=true` подтверждает только
  успешную трансляцию, а не сохранение семантики.
- В каноническую матрицу добавлены два негативных иерархических кейса. Все 46
  кейсов заново выполнены с инспектором `1.1.0`; итог: 37 ожидаемых результатов,
  7 ошибок и 2 подтверждённых опасных поведения.
- Все 42 сформированных SQL проверены в WTDB: 39 прошли компиляцию и выполнение
  `countSql`, ошибки остались только у трёх форм отрицания `MatchSome()`.
- Состояние `doc-contains()` актуализировано: полнотекстовый индекс
  `dbo.collaborator(data)` включён, заполнен и работает в режиме `AUTO`; обычный
  полнотекстовый кейс теперь проходит.
- Причина: зафиксировать риск полного чтения каталога и не считать успешную
  трансляцию достаточным результатом автоматического теста.
- Проверки: полный прогон XQuery через серверный endpoint, компиляция и
  выполнение SQL в MSSQL, сверка канонической матрицы и отчёта. Визуальная
  проверка не выполнялась; dev-сервер и стенд не запускались, не
  перезапускались и не останавливались.

## 10.09.2026 14:12 — Редактура эталонной статьи 434_MSSQL

- Статья полностью проверена как основа для следующих сборок и СУБД:
  уточнено происхождение SQL, выровнены формулировки, удалены хвостовые пробелы
  и табуляции, сохранён единый формат XQuery, SQL, параметров и нюансов.
- Раздел явных соединений дополнен отдельными запросами `join`, `ljoin`,
  `rjoin` и антисоединения. По результатам стенда и анализа DLL явно указано,
  что парсер XQuery `1.22.6.8` этот синтаксис не поддерживает; рабочие способы
  соединения каталогов отделены от него.
- Вставленные SQL для `IsHierChild()` и `IsHierChildOrSelf()` выполнены в
  MSSQL и совпали с инспекцией уже подготовленных `Hier(ID, '-')` и
  `Hier(ID, '+')` после нормализации форматирования.
- Для иерархических примеров пояснены источник Mode Debug, служебные параметры
  `@p1`, дополнительные поля `__hcc`/`__hlevel` и сортировка через
  `__sort_level`. Подготовленные инспекции сохранены во внутреннем отчёте.
- В раздел сортировки возвращён вывод об отсутствии неявного
  `order by t_elem.id` в проверенных запросах provider 434.
- Причина: зафиксировать понятную и технически согласованную версию
  `434_MSSQL`, структуру и канонические XQuery которой можно повторять на
  других стендах.
- Проверки: три иерархических SQL выполнились в WTDB; 44 XQuery согласованы с
  отчётом; локальные ссылки, 148 границ блоков кода, строгая сборка и
  `git diff --check` проверены. Визуальная проверка не выполнялась;
  dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 13:57 — Фактический SQL иерархических XQuery

- Для `IsHierChild()` промежуточный SQL инспектора заменён на фактически
  выполненный SQL из журнала Mode Debug: CTE начинается с
  `parent_object_id=@p0`, а вызов `ishierchild()` в итоговой команде отсутствует.
- Явно показано преобразование `tools.xquery()`: `IsHierChild()` переносит ID
  в `/Hier(ID, '-')`, а `IsHierChildOrSelf()` — в `/Hier(ID, '+')`; `-`
  исключает исходный узел, `+` включает его.
- Добавлен самостоятельный пример `IsHierChildOrSelf()` без дополнительного
  фильтра и SQL-блок для последующей вставки результата Mode Debug. SQL
  текущего инспектора явно отмечен как неприменимый к этому примеру.
- Канонический XQuery и структурированный отчёт обновлены; новый вариант
  `IsHierChildOrSelf()` выполнился через `tools.xquery()` и вернул строки.
- Причина: отделить фактически исполняемый иерархический SQL от результата
  прямого вызова provider, обходящего подготовку `tools.xquery()`.
- Проверки: согласованность 44 кейсов, 144 границы блоков кода, строгая сборка
  и `git diff --check` проверены. Визуальная проверка не выполнялась;
  dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 13:44 — Проверка сортировки на двух строках

- Кейсы `order by` больше не фильтруются по одному ID. Для `ASC` и `DESC`
  используются два тестовых сотрудника с кодами `13744` и `109295` через
  `MatchSome()`.
- В статье приведены полные XQuery, SQL, оба параметра и фактический порядок:
  `ASC` — Анисимов, Вилкова; `DESC` — Вилкова, Анисимов.
- Обновлены канонические запросы и структурированный результат стенда
  `434_MSSQL`; оба направления выполнились и подтвердили ожидаемый порядок.
- Причина: проверять не только наличие `ASC`/`DESC` в SQL, но и реальную
  перестановку нескольких строк.
- Проверки: XQuery выполнены через `tools.xquery()`, сформированный SQL — через
  MSSQL; согласованность 44 кейсов, строгая сборка и `git diff --check`
  проверены. Визуальная проверка не выполнялась; dev-сервер и стенд не
  запускались и не перезапускались.

## 10.09.2026 13:07 — Терминология каталога appointment_types

- Из статьи `434 + MSSQL` и внутренних пояснений удалено ошибочное утверждение,
  что `appointment_types` является только справочником интерфейса.
- `appointment_types` используется как полноценный каталог XQuery; отдельное
  пояснение его связи с `positions.position_appointment_type_id` не требуется.
- Причина: не искажать модель каталогов WebSoft и не усложнять рабочий пример
  трёхтабличного `some/satisfies`.
- Проверки: строгая сборка и `git diff --check` прошли. Визуальная проверка не
  выполнялась; dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 13:02 — Параметры SQL-примеров XQuery

- К примеру двух каталогов добавлен фактический параметр
  `@p0 BigInt = 1105387902724063510`.
- Проверены остальные параметризованные блоки статьи; недостающие значения и
  типы добавлены для `some/satisfies`, трёх каталогов, отрицания
  `MatchSome()`, `ForeignElem()`, `IsHierChild()` и сортировки.
- Причина: обеспечить единый формат `XQuery → SQL → параметры` для всех
  примеров, содержащих `@p0` или `@p1`.
- Проверки: параметры сверены со структурированным стендовым отчётом, строгая
  сборка и `git diff --check` прошли. Визуальная проверка не выполнялась;
  dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 12:59 — Параметры группировки and/or

- В примере с группировкой `and` и `or` явно указаны оба фактических
  параметра: строковая маска `@p0 VarChar = %Анисимов%` и числовой
  `@p1 BigInt = 1105387902724063521`.
- Причина: привести пример к общей структуре XQuery, SQL и значений
  параметров.
- Проверки: строгая сборка и `git diff --check` прошли. Визуальная проверка не
  выполнялась; dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 01:22 — Примеры отрицаний простых значений XQuery

- Раздел `false(), true(), null(), пустая строка и IsEmpty()` дополнен
  самостоятельными парами XQuery/SQL для равенства и отрицания; прежняя
  таблица сохранена как краткое сравнение.
- На стенде `434_MSSQL` подтверждены `= true()`, `!= false()`, `!= true()`,
  `!= null()`, `!= ''`, `IsEmpty(...)=false()` и
  `IsEmpty(...)!=true()`. Все запросы вернули ожидаемые строки, основной SQL
  скомпилирован, count SQL выполнен.
- Зафиксирована асимметрия булевых условий: `= false()` и `!= true()` включают
  `NULL`, тогда как `= true()` и `!= false()` его не включают. Пустая строка
  преобразуется как `NULL` в обе стороны сравнения.
- Каноническая матрица расширена с 37 до 44 кейсов, структурированный отчёт и
  внутреннее описание обновлены. Итог: 36 семантически успешных и 8
  проблемных кейсов.
- Причина: заменить абстрактное резюме конкретными проверенными примерами и
  перенести новые проверки на будущие стенды без изменения XQuery.
- Проверки: согласованность 44 запросов и результатов, JSON, блоки кода,
  строгая сборка и `git diff --check` проверены. Визуальная проверка не
  выполнялась; dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 01:03 — Понятные литералы в XQuery-примерах

- Служебные значения `XQI-NOT-FOUND`, `XQI-A` и `XQI-B` удалены из статьи и
  внутренних XQuery-кейсов. В примерах `contains()` используется фамилия
  `Анисимов`, в `MatchSome()` — существующие тестовые коды `13744` и `109295`.
- Положительные и отрицательные варианты теперь используют одинаковые
  значения, поэтому различие результата объясняется самой операцией, а не
  искусственными литералами.
- Четыре затронутых запроса повторно выполнены на стенде `434_MSSQL`;
  `contains(...)=false()` вернул ожидаемую строку, три отрицания `MatchSome()`
  повторили прежний ошибочный пустой результат. Структурированный отчёт
  обновлён фактическими SQL и параметрами.
- Причина: сделать пользовательские примеры понятнее без расхождения с
  автоматической матрицей следующих стендов.
- Проверки: отсутствие `XQI-*`, согласованность 37 запросов, строгая сборка и
  `git diff --check` проверены. Визуальная проверка не выполнялась;
  dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 01:00 — Параметр отрицательного contains

- В примере `contains(...)=false()` для сборки `434 + MSSQL` явно указано
  фактическое значение параметра с добавленной provider маской:
  `@p0 VarChar = %XQI-NOT-FOUND%`.
- Причина: привести положительный и отрицательный примеры `contains()` к
  одинаковой структуре объяснения.
- Проверки: строгая сборка и `git diff --check` прошли. Визуальная проверка не
  выполнялась; dev-сервер и стенд не запускались и не перезапускались.

## 10.09.2026 00:55 — Требование полнотекстового индекса для doc-contains

- В статье `434 + MSSQL` явно разделены плоский каталог `dbo.collaborators` и
  документная таблица `dbo.collaborator`, в которой обычный `doc-contains()`
  выполняет `CONTAINS` по XML-колонке `data` при `LuceneFTIndex=False`.
- Зафиксировано состояние стенда: компонент SQL Server Full-Text Search
  установлен, но полнотекстового индекса для `dbo.collaborator` нет; доступен
  уникальный ключ `PK__collaborator` по `id`.
- Добавлена фактическая ошибка SQL 7601 и требование повторить кейс после
  создания полнотекстового индекса на `dbo.collaborator.data`. XML-варианты
  поиска по кастомным полям оставлены отдельным механизмом `data.exist()`.
- Причина: не смешивать наличие компонента Full-Text Search с готовностью
  конкретной таблицы к использованию `CONTAINS`.
- Проверки: строгая сборка, локальные ссылки, 96 границ блоков кода и
  `git diff --check` проверены. Изменения БД не выполнялись; визуальная
  проверка не проводилась, dev-сервер и стенд не перезапускались.

## 10.09.2026 00:42 — Семантическая проверка XQuery на тестовых данных

- Канонические 37 XQuery привязаны к известным тестовым сотрудникам,
  должностям, подразделениям, кастомным полям и группам; для каждого кейса
  добавлено ожидаемое наличие строки `expectedHasRows`.
- Подтверждено, что `positions.position_appointment_type_id` — справочное
  ссылочное поле `foreignArray="appointment_types"`. После заполнения связи
  позитивный трёхтабличный `some/satisfies` сформировал два `INNER JOIN` и
  вернул ожидаемую строку.
- Полный прогон дал 29 совпадений из 37. Четыре явных соединения завершились
  исключением; три формы отрицания `MatchSome()` и обычный `doc-contains()`
  вернули пустую коллекцию вместо ожидаемой строки.
- Положительный `MatchSome()`, `ForeignElem()`, XML-варианты `doc-contains()`,
  `IsHierChild()`, `IsHierChildOrSelf()`, `CatalogHierSubset()`, условия,
  проекции и сортировка дали ожидаемый результат.
- Статья и внутренний README исправлены: отсутствие исключения больше не
  считается достаточным успехом, тестовые литералы заменены проверяемыми
  значениями, а справочная связь `appointment_types` объяснена рядом с
  примером.
- Структурированный отчёт обновлён до схемы 2: отдельно сохранены инспекция,
  выполнение `tools.xquery()`, семантический вердикт и проверка SQL в MSSQL.
- Проверки: основной SQL скомпилирован и count SQL выполнен для 27 из 33
  сформированных команд; JSON, ссылки, строгая сборка и `git diff --check`
  проверены. Визуальная проверка не выполнялась; dev-сервер и стенд не
  запускались и не перезапускались.

## 10.09.2026 00:08 — Выполнение XQuery через тестовый HTTP endpoint

- На стенде `434_MSSQL` проверен созданный настраиваемый шаблон
  `xquery-case-api` с ID `7683642904943435809`: GET health, POST с XQuery,
  обработка пустого запроса и обязательная cookie-авторизация работают.
- Через endpoint выполнены все 37 канонических запросов. `tools.xquery()`
  успешно завершил 33 кейса; `join`, `ljoin`, `rjoin` и антисоединение через
  `ljoin` с `MatchSome()` завершились ошибкой.
- Подтверждена дополнительная обработка штатного пути: отрицания
  `MatchSome()`, обычный `doc-contains()`, `IsHierChild()` и
  `IsHierChildOrSelf()` выполнились, хотя SQL прямой инспекции не проходил
  самостоятельную проверку MSSQL.
- Статья `434 + MSSQL` исправлена по результатам полного выполнения: прямой
  SQL отделён от фактического пути `tools.xquery()`, ограничения соединений
  подтверждены, а выводы по `MatchSome()`, полнотекстовому поиску и иерархии
  уточнены.
- Код шаблона, его внутренний HTTP-контракт и компактные результаты выполнения
  сохранены только в `resources/drafts/xquery-to-sql/`; учетные данные, cookie,
  значения строк и полные журналы в проект не добавлялись.
- Проверки: ответы endpoint разобраны как JSON, пустой POST вернул HTTP 400,
  анонимный GET перенаправлен на вход. Строгая сборка, ссылки и
  `git diff --check` проверены; визуальная проверка не выполнялась, dev-сервер
  и стенд не запускались и не перезапускались.

## 09.09.2026 23:34 — Упрощение статьи о XQuery и SQL

- Страница `434 + MSSQL` переписана как практическая последовательность
  `XQuery → SQL → ограничение или нюанс`; удалены ID кейсов, сводная матрица,
  статистика прогонов, методика инспекции и повторяющиеся служебные статусы.
- Одинаковые примеры объединены, SQL отформатирован для чтения, а ошибки
  `join/ljoin/rjoin`, отрицания `MatchSome()`, полнотекстового поиска и
  иерархических функций оставлены рядом с соответствующими конструкциями.
- Публикуемые JSON с каноническими запросами и стендовыми результатами удалены
  из `docs/`. Они сохранены только в `resources/drafts/xquery-to-sql/` для
  подготовки идентичных статей по остальным стендам.
- Общая страница раздела сокращена до списка окружений, правила одинаковых
  XQuery и ссылки на документацию `tools.xquery()`.
- Причина: отделить понятную пользовательскую статью от внутреннего протокола
  исследования и материалов автора.
- Проверки: локальные ссылки и 94 границы блоков кода проверены; в публикуемом
  разделе нет JSON; строгая сборка — `No issues found`, `git diff --check`
  прошёл. Визуальная проверка не выполнялась; dev-сервер и стенд не
  запускались и не перезапускались.

## 09.09.2026 23:07 — Разбор XQuery для сборки 434 и MSSQL

- Опубликованы общий обзор преобразования XQuery в SQL и первый раздел для
  WebSoft HCM Server `2022.1.3.434`, MSSQL provider `1.22.6.8` и Microsoft SQL
  Server `16.0.4275.2`; страницы добавлены в каталоги, главную и навигацию.
- Исходные примеры переработаны в каноническую матрицу из 37 неизменяемых
  XQuery. Добавлены отдельные проверки числового и строкового параметров,
  группировки `and/or`, двух форм отрицания `MatchSome()` и сортировки
  `ASC`/`DESC`; реальные значения заменены нейтральными литералами.
- Инспектор сформировал SQL для 33 запросов. Четыре явных соединения завершились
  `NullReferenceException` до получения SQL. Для 27 запросов основной SQL
  скомпилирован и count SQL выполнен; три формы отрицания `MatchSome()`, обычный
  `doc-contains()` без полнотекстового индекса и два иерархических SQL не прошли
  прямую проверку MSSQL.
- Уточнены границы доказательности: `tools.xquery()` отдельно не выполнялся,
  иерархический SQL признан промежуточным, а предупреждение инспектора для
  допустимых MSSQL-псевдонимов `[field][alias]` — ложным срабатыванием.
- Полные запросы, SQL, count SQL, параметры и ошибки сохранены как JSON рядом
  со статьёй и в черновых ресурсах; учетные данные, cookie, значения каталогов
  и полные HTTP-журналы не сохранялись.
- Проверки: два JSON-файла разобраны и сопоставлены с опубликованными копиями,
  проверены локальные ссылки и 148 границ блоков кода; строгая сборка —
  `No issues found`, `git diff --check` прошёл. Визуальная проверка не
  выполнялась; dev-сервер и стенд не запускались и не перезапускались.

## 09.09.2026 22:12 — Первый стендовый результат XQuery Inspector

- В `resources/drafts/xquery-to-sql/stand-results/` сохранён структурированный
  результат базового запроса на стенде `434_MSSQL`: окружение, существенные
  настройки, XQuery, SQL, count SQL, параметры и оценки инспектора.
- Подтверждено совпадение развёрнутых DLL и шаблона с актуальными файлами;
  запрос через cookie-авторизацию завершился успешно без предупреждений и
  ошибки очистки. Учетные данные и HTTP-журналы не сохранялись.
- Причина: начать воспроизводимую матрицу одинаковых запросов для четырёх
  сочетаний сборки WebSoft HCM и СУБД.
- Проверки: JSON разобран, строгая сборка и `git diff --check` выполнены. SQL
  извлечён на стенде, но не выполнялся как пользовательская команда;
  визуальная проверка не проводилась, dev-сервер и стенд не перезапускались.

## 09.09.2026 19:27 — Черновик о пользовательских C#-библиотеках

- В `resources/drafts/custom-csharp-library/` сохранён черновик будущего
  рецепта: сборка и размещение DLL, загрузка через `tools.dotnet_host`, вызов
  классов и статических методов, C#-агенты, директива `//#r`, диагностика и
  матрица оставшихся проверок.
- Зафиксировано подтверждённое ограничение сборки 434: имена assembly с
  префиксами `Datex.` и `Websoft.` требуют фирменный public key token, а отказ
  маскируется под `FileNotFoundException`.
- Причина: сохранить воспроизводимый опыт исследования вне публикуемого сайта
  до проверки на сборке 906 и подготовки компактного рецепта.
- Проверки: отсутствие черновика в `docs/`, строгая сборка, локальные ссылки и
  `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не
  запускался и не перезапускался.

## 09.09.2026 01:09 — Разбор WebSoft.RegExp для сборок 434 и 906

- Создан единый разбор WebSoft.RegExp `1.22.6.8` и `1.24.5.14`: подключение, состояние общего объекта, экранирование, флаги, поиск, замена, коллекции, группы, ошибки, ограничения и таблица API. Разбор добавлен в каталог, главную страницу и навигацию.
- Исходники DLL и одинаковые серверные агенты подтвердили совпадение проверенного поведения. Единственное отличие сборок — отсутствие `IsMatch()` в 434 и его наличие в 906. Исправлены ошибки исходной статьи и добавлена отсутствовавшая перегрузка `Execute(source, pattern)`.
- Из статьи удалены служебные подробности проверок; результаты примеров сохранены без времени и ID потоков. Временные агенты, протоколы и журналы удалены из `resources/` после переноса подтверждённых выводов.
- Проверки: 9 JavaScript-блоков синтаксически разобраны; локальные ссылки, строгая сборка и `git diff --check` прошли. Визуальная проверка не выполнялась; dev-сервер не запускался и не перезапускался.

## 08.09.2026 22:12 — Перенос Websoft.Zip в разборы

- Раздел Websoft.Zip перенесён из руководств в разборы: обновлены расположение страниц, навигация, каталоги и карточки главной страницы.
- Вводные формулировки страниц приведены к типу материала. Причина: материал преимущественно сравнивает API, поведение, ограничения и различия сборок, а не описывает единый последовательный процесс.
- Проверки: строгая сборка, локальные ссылки перенесённого раздела и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не запускался и не перезапускался.

## 08.09.2026 22:09 — Содержимое разделов на главной

- В карточки рецептов и разборов добавлено явное сообщение об отсутствии публикаций, а в карточку руководств — ссылки на материалы о WebSocket и Websoft.Zip.
- Удалён блок «Состояние справочника», который дублировал содержимое карточки руководств.
- Проверки: строгая сборка, ссылки главной страницы и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не запускался и не перезапускался.

## 08.09.2026 22:02 — Редактура руководства Websoft.Zip

- Обзор и страницы сборок 434 и 906 повторно сверены с протоколами матрицы путей. Предупреждения сохранены: сборка 434 извлекает все проверенные опасные пути, а сборка 906 блокирует абсолютный и двухуровневые пути, но пропускает одноуровневый `../`.
- Убраны подробности тестовой кухни: упоминания последовательных агентов, неудачной подготовки файлов, точных размеров пробных архивов, внутреннего условия обхода защиты и непроверенного частичного сценария. Практические ограничения API и условия проверки сохранены.
- Проверки: строгая сборка и локальные ссылки раздела прошли; `git diff --check` выполнен. Визуальная проверка не проводилась; dev-сервер не запускался.

## 08.09.2026 21:24 — Вводная формулировка Websoft.Zip

- Во вводном разделе об общем поведении убраны дата и упоминание тестовых агентов. Формулировка сосредоточена на подтверждённых особенностях обеих сборок.
- Проверки: строгая сборка и `git diff --check` выполнены. Визуальная проверка не проводилась; dev-сервер не запускался.

## 08.09.2026 21:08 — Руководство по Websoft.Zip для сборок 434 и 906

- Созданы обзор выбора версии и две самостоятельные статьи для WebSoft HCM Server `2022.1.3.434` с Websoft.Zip `1.22.6.8` и `2023.2.906` с Websoft.Zip `1.24.5.14`. Руководство добавлено в каталог и навигацию.
- В статьи перенесены подтверждённые сценарии создания, добавления, чтения и распаковки архивов; описаны фактические режимы сжатия, кодировки имён, сохраняемое состояние, устаревающий `GetError()`, неоднозначные коды возврата и тип `System.IO.FileAccess` у `OpenArchive()`.
- Для обеих сборок добавлено предупреждение о распаковке только доверенных архивов. В статье 434 зафиксировано отсутствие проверки путей, в статье 906 — блокировка двухуровневого, смешанного и абсолютного пути при сохранении уязвимости для одноуровневого `../`.
- Старые скриншоты не переносились: они не подтверждают результаты новых стендов. Непроверенные после перезапуска WebTutor состояния и совместимость кодировок со сторонними распаковщиками явно отделены от результатов испытаний.
- Проверки: строгая сборка — `No issues found`; 20 JavaScript-блоков синтаксически разобраны; локальные ссылки новых страниц разрешаются; `git diff --check` прошёл. Визуальная проверка и повторное выполнение примеров на стендах не проводились; dev-сервер не запускался.

## 08.09.2026 20:56 — Матрица путей Websoft.Zip 434

- Агент `06-path-matrix.js` на DLL `1.22.6.8` извлёк все пять записей: обычную, одноуровневую `../`, двухуровневую `../../`, смешанную `sub/../../` и абсолютную. Все вызовы `ExtractFiles()` вернули `1`, файлы созданы, `GetError()` оставался пустым.
- Результат подтверждает отсутствие проверок путей в декомпилированной реализации 434. В отличие от неё DLL 906 блокирует проверенные двухуровневый, смешанный и абсолютный пути, но остаётся уязвимой для одноуровневого `../`.
- Протоколы и матрица обновлены; основная серия одинаковых тестов на обеих сборках завершена. Следующий этап — самостоятельные статьи для сборок 434 и 906.
- Проверки: журналы 434 сопоставлены по времени `20:55:56` и потоку `[0064]`; строгая сборка и `git diff --check` выполнены. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:51 — Дополнительные проверки Websoft.Zip 906

- Агент `05-content-details.js` подтвердил совпадение с DLL 434: пустой каталог не сохраняется; уровень `0` создал ZIP размером 4238 байт без сжатия, уровни `1` и `7` — одинаковые ZIP по 170 байт с Deflate; сама DLL прочитала кириллическое имя в `windows-1251` и `utf-8`.
- Запущенный пользователем `06-path-matrix.js` уточнил границу защиты DLL `1.24.5.14`: обычный файл и одноуровневый `../one.txt` извлечены, а `../../two.txt`, `sub/../../mixed.txt` и абсолютный путь отклонены без создания файлов. Три отклонения записаны в журнал `zip_`.
- Протокол и матрица 906 обновлены. Результат сопоставлен с декомпилированной реализацией `ExtractFiles()`: проверка абсолютного пути работает, но поиск `../` в имени каталога пропускает путь, заканчивающийся на `..`. Следующий шаг — выполнить тот же агент на сборке 434.
- Проверки: журналы 906 сопоставлены по времени `20:41:10` и потоку `[0502]`; строгая сборка и `git diff --check` выполнены. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:31 — Неполная защита путей Websoft.Zip 906

- `04-traversal.js` подтвердил, что DLL `1.24.5.14`, как и 434, извлекает `../escape.txt` за пределы переданного каталога, возвращает `1` и не создаёт новую ошибку.
- Результат сопоставлен с декомпилированным кодом: проверка 906 ищет `..` с последующим разделителем в имени каталога, но для `../escape.txt` вычисленный каталог заканчивается на `..`, поэтому условие не срабатывает. Защита 906 признана неполной, а не отсутствующей целиком.
- Подготовлен безопасный `06-path-matrix.js` для отдельных проверок обычного, одноуровневого, двухуровневого, смешанного и абсолютного пути. Все возможные цели расположены внутри `x-local://trash/websoft-zip-validation/path-matrix/`.
- Проверки: строгая сборка — `No issues found`; агент прошёл `node --check`; встроенный ZIP разобран в памяти и содержит ровно пять запланированных записей; журнал 906 сопоставлен по времени `20:25:44` и потоку `[0495]`; `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:24 — Состояние Websoft.Zip между агентами 906

- Последовательные агенты на сборке 906 подтвердили сохранение `CharSet = windows-1251`, `CompressionLevel = 7` и последней ошибки между запусками без перезапуска сервера.
- `03-state-read.js` восстановил кодировку `Unicode (UTF-8)` и уровень `1`; результат совпал со сборкой 434. Ошибка `OpenArchive()` записана в штатный журнал `zip_`.
- Протокол и матрица 906 обновлены. Сохранение состояния после перезапуска WebTutor отдельно не проверялось и не считается подтверждённым.
- Проверки: строгая сборка — `No issues found`; записи сопоставлены по времени `20:22:50`/`20:23:01`, потокам `[0435]`/`[0436]` и журналам `websoft_zip_validation`, `zip_`, `xhttp_`, `agent_`; `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:21 — Успешный основной прогон Websoft.Zip 906

- Исправленный `01-core-behavior.js` подтвердил существование входных файлов по URL и файловому пути с длинами `5`, `0`, `4` байта; весь основной набор операций DLL `1.24.5.14` завершился успешно.
- Одиночный архив и архив по маске содержат `alpha.txt`, рекурсивный — `alpha.txt` и `nested/beta.txt`; пустой файл пропущен. Полная и выборочная распаковка работают, отсутствующий файл не создаётся при возврате `1`.
- Для проверенных операций поведение совпало со сборкой 434. Пустые архивы первого прогона подтверждены как дефект подготовки тестовых файлов, а не различие DLL. Протокол и матрица 906 обновлены.
- Проверки: строгая сборка — `No issues found`; журнал `websoft_zip_validation` разобран по времени `20:19:38` и потоку `[0364]`, входные данные подтверждены диагностическими строками; `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:18 — Файловая подготовка тестов Websoft.Zip 906

- Повторный запуск основного агента остановился на `LoadUrlData()`: подготовленный через `PutUrlData()` URL не был найден до обращения к DLL. Ошибка отнесена к тестовой инфраструктуре, а не к `Websoft.Zip.dll`.
- Агенты `01`, `04` и `05` переведены на единый файловый путь: каталоги создаются через `ObtainDirectory(UrlToFilePath(...), true)`, бинарные данные записываются через `PutFileData()`, читаются через `LoadFileData()`.
- Пустой файл создаётся бинарной строкой `Base64Decode('')`; непустые данные также формируются через Base64, чтобы исключить различия представления строк между сборками.
- Проверки: строгая сборка — `No issues found`; три изменённых агента прошли `node --check`, `git diff --check` прошёл. Запуск исправленной версии на 906 ещё не выполнен; публикуемые страницы и dev-сервер не изменялись, визуальная проверка не выполнялась.

## 08.09.2026 20:13 — Первый основной прогон Websoft.Zip 906

- Подтверждены DLL `1.24.5.14`, исходные значения свойств, журналирование ошибок в `zip_`, поведение `ListFiles()` до `Close()`, неочевидные коды возврата без открытого архива, сохранение `GetError()` после успеха и отклонение числового `FileAccess` слоем SP-XML.
- Созданные архивы оказались пустыми. Результат не признан отличием DLL, поскольку код добавления в 434 и 906 совпадает, а исходные файлы готовились передачей обычного текста в `PutUrlData()`, предназначенную для бинарной строки.
- `01-core-behavior.js` и `05-content-details.js` переведены на явные бинарные данные через `Base64Decode()`; основной агент дополнен проверкой URL, файлового пути и длины входов. Протокол и матрица 906 отмечают необходимость повторного запуска.
- Проверки: строгая сборка — `No issues found`; оба изменённых агента прошли `node --check`; журналы `websoft_zip_validation`, `zip_`, `xhttp_` и `agent_` сопоставлены по времени `20:08:56` и потоку `[0127]`; `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:05 — Дополнительные проверки Websoft.Zip 434

- Агент `05-content-details.js` подтвердил, что DLL `1.22.6.8` не сохраняет пустой каталог; рекурсивный архив содержит только файлы и их относительные пути.
- Для источника из 4096 одинаковых байт значение `CompressionLevel = 0` создало ZIP размером 4238 байт, а значения `1` и `7` — одинаковые ZIP по 170 байт. Свойство возвращает число `7`, но фактически любое проверенное ненулевое значение выбирает Deflate.
- DLL успешно прочитала кириллическое имя после создания и повторного открытия архивов с `windows-1251` и `utf-8`. Совместимость со сторонними распаковщиками не проверялась и не считается подтверждённой.
- Протокол и матрица 434 обновлены; свойства после теста восстановлены в `utf-8` и уровень `1`. Проверки: строгая сборка — `No issues found`; журнал сопоставлен по времени `20:03:54` и потоку `[0146]`, ошибок в `xhttp` нет, `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 20:02 — Проверка обхода пути Websoft.Zip 434

- Агент `04-traversal.js` подтвердил, что DLL `1.22.6.8` извлекает запись `../escape.txt` уровнем выше переданного каталога назначения, возвращает `1` и не создаёт новую ошибку. Тестовая запись осталась внутри выделенного каталога `x-local://trash/websoft-zip-validation/`.
- Старый текст `GetError()` сохранился после успешной распаковки, поэтому непустое значение свойства нельзя связывать с последней операцией без проверки её кода возврата.
- Подготовлен `05-content-details.js` для оставшихся проверок пустого каталога, уровней сжатия `0`/`1`/`7` и кириллического имени в `windows-1251`/`utf-8`; план, матрица и протокол 434 обновлены.
- Проверки: строгая сборка — `No issues found`; новый агент прошёл `node --check`; журналы теста сопоставлены по времени `19:59:31` и потоку `[0157]`; встроенный архив перечислил только `safe.txt` и `../escape.txt`; `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 19:56 — Состояние Websoft.Zip между агентами 434

- Последовательные запуски `02-state-write.js` и `03-state-read.js` без перезапуска WebTutor подтвердили сохранение между серверными агентами кодировки `Cyrillic (Windows)`, уровня сжатия `7` и текста последней ошибки.
- После чтения агент восстановил `CharSet` в `Unicode (UTF-8)` и `CompressionLevel` в `1`; штатного способа очистить `GetError()` не обнаружено.
- Результат зафиксирован только для последовательных агентов сборки 434. Сохранение состояния после перезапуска сервера не проверялось и не считается подтверждённым.
- Проверки: строгая сборка — `No issues found`; записи сопоставлены по времени `19:55:23`/`19:55:30` и потокам `[0154]`/`[0155]`; ошибка `OpenArchive()` подтверждена стеком в `xhttp_`; `git diff --check` прошёл. Публикуемые страницы и dev-сервер не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 19:50 — Повторный основной прогон Websoft.Zip 434

- Повторный запуск уточнённого `01-core-behavior.js` подтвердил состав архивов после `Close()` и повторного открытия: одиночный и созданный по маске архивы содержат `alpha.txt`, рекурсивный — `alpha.txt` и `nested/beta.txt`; пустой файл во всех случаях пропущен.
- Подтверждено, что `ListFiles()` до закрытия создаваемого архива возвращает `undefined` с ошибкой об отсутствии центрального каталога, а после повторного открытия возвращает список.
- Протокол и матрица 434 обновлены; отдельно зафиксировано, что пустые каталоги, фактический режим сжатия и кодировка имён ещё требуют воспроизводимых тестов.
- Проверки: строгая сборка — `No issues found`; журналы `websoft_zip_validation` и `xhttp_` сопоставлены по времени `19:47:54`, потоку `[0131]` и стеку DLL; `git diff --check` прошёл. Публикуемые страницы не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 19:33 — Первый прогон Websoft.Zip на сборке 434

- Разобраны журналы первого запуска `01-core-behavior.js`: подтверждены DLL `1.22.6.8`, исходные `CharSet` и `CompressionLevel`, операции добавления и распаковки, пропуск пустого файла, неочевидные успешные коды без выполненной операции и сохранение `GetError()` после успеха.
- Подтверждено внутреннее журналирование DLL 434 в `xhttp_`; числовые значения второго параметра `OpenArchive()` отклоняются слоем вызова SP-XML до исполнения DLL.
- Тест уточнён: `ListFiles()` теперь показывает отдельно ошибку до закрытия создаваемого архива и список после `Close()` и повторного открытия. В протоколе 434 зафиксированы результаты и необходимость повторного запуска обновлённого агента.
- Проверки: строгая сборка — `No issues found`; обновлённый агент прошёл `node --check`, `git diff --check` прошёл. Статьи и публикуемые страницы не изменялись; визуальная проверка не выполнялась.

## 08.09.2026 19:03 — Подготовка проверки Websoft.Zip по сборкам

- В исходном материале `resources/websoft-guide/docs/materials/dotnet-websoft-zip/validation/` зафиксированы план переноса, матрица проверок и шаблоны результатов для WebSoft HCM Server 434 и 906.
- Подготовлены четыре серверных агента: основной функциональный прогон, запись и чтение состояния между запусками, безопасная проверка пути `../` внутри ZIP. Агенты работают только в `x-local://trash/websoft-zip-validation/` и пишут результаты с префиксом `WSZIP` в отдельный журнал.
- Причина: сначала получить одинаковые воспроизводимые результаты на обоих стендах, затем написать самостоятельные статьи для DLL `1.22.6.8` и `1.24.5.14`.
- Проверки: строгая сборка — `No issues found`; синтаксис четырёх JS-файлов проверен через `node --check`; встроенный тестовый ZIP разобран в памяти и содержит только `safe.txt` и `../escape.txt`; `git diff --check` прошёл. Запуски на WebTutor и визуальная проверка ещё не выполнялись; публикуемые страницы не создавались.

## 03.09.2026 22:03 — Начальный каркас

- Создан отдельный проект WebSoft Handbook на Zensical с управлением зависимостями через uv.
- Добавлены главная с карточками, каталоги рецептов, разборов и руководств, светлая и тёмная темы.
- Добавлены инструкция автора, шаблон рецепта, правила работы.
- Причина: создать удобный практический справочник.
- Проверки: оба TOML-файла успешно разобраны; все 4 страницы включены в навигацию, все 3 относительные ссылки страниц ведут на существующие файлы.
- `uv sync` вне песочницы завершился тайм-аутом загрузки метаданных Zensical с `files.pythonhosted.org` после повторных попыток. Создана только локальная `.venv`, зависимости не установлены, `uv.lock` не создан. Полная сборка не проверена; первая установка описана в README.
- Dev-сервер, визуальная проверка и публикация не выполнялись.

## 03.09.2026 22:09 — Установка зависимостей и запуск dev-сервера

- По прямому запросу пользователя повторена установка зависимостей: `uv sync` успешно установил Zensical 0.0.59 и создал `uv.lock`.
- Обновлён README: снята отметка о блокировке установки, команды используют lock-файл.
- `uv run --locked zensical build --strict` завершился успешно: `No issues found`.
- Запущен `uv run --locked zensical serve` на `http://localhost:8000`; до запуска порт был свободен.
- HTTP-проверка: главная, `/recipes/`, `/explanations/` и `/guides/` вернули 200; заголовок главной — `Главная - WebSoft Handbook`.
- Визуальная проверка не выполнялась. Публикация не выполнялась.

## 03.09.2026 22:13 — Начало истории Git

- По запросу пользователя инициализирован отдельный локальный репозиторий, ветка `codex/initial-scaffold`.
- Подготовлен первый коммит каркаса и `uv.lock`; `.venv/`, `site/` и `.cache/` исключены через `.gitignore`.
- Причина: зафиксировать работающий каркас перед добавлением статей.
- Контрольная строгая сборка прошла без ошибок. Просмотренные исходники не содержат обнаруженных ключей, паролей или приватных ключей.
- Удалённый репозиторий не подключался, push не выполнялся; dev-сервер не перезапускался, визуальная проверка не выполнялась.

## 03.09.2026 22:30 — Руководство по WebSocket

- Руководство разделено на обзор, подключение, сообщения, группы и теги; полный HTML-клиент размещён отдельной шестой страницей. Добавлены 23 изображения.
- Добавлены навигация, ссылки с главной и каталога, переходы между главами. Причина: проверить новую структуру на первом большом материале, сохранив варианты версий внутри соответствующей главы.
- Указан статус проверки; расхождение обозначений сборки 906 явно отмечено, версии не исправлялись предположением. Правила webtutor-platform использованы для сохранения серверных примеров без изменения логики; исправлены только оформление и явные опечатки пояснений.
- Проверки: строгая сборка `uv run --locked zensical build --strict` — `No issues found`; проверены 26 блоков кода, полный HTML-клиент и целостность 23 изображений; проверены 44 локальные ссылки, изображения и якоря в собранных страницах; `git diff --check` и проверка пробелов новых страниц прошли.
- Визуальная проверка и выполнение на стенде WebTutor не проводились. Требуется визуальная проверка. Dev-сервер не запускался повторно, не перезапускался и не останавливался. Коммит, push и публикация не выполнялись.

## 03.09.2026 22:34 — Редакционная очистка

- По запросу пользователя удалён служебный план, упрощены главная, README, правила проекта и пояснения руководства. Журнал приведён к описанию самого справочника, без контекста его предыдущей организации.
- Сохранены предупреждения о версиях и необходимости проверки на стенде. Код, HTML-клиент и 23 изображения не изменены.
- Проверки: строгая сборка — `No issues found`; проверены 49 локальных ссылок, изображений и якорей; выполнен поиск удалённых формулировок в текстах и собранном HTML; `git diff --check` прошёл.
- Визуальная проверка и запуск на стенде не выполнялись; dev-сервер не перезапускался.

## 03.09.2026 22:48 — Удаление служебных статусов из руководства

- Удалены блоки «Статус материала» из пяти глав и общий «Статус проверки» из обзора. Заголовок раздела и описание в каталоге приведены к сведениям о версиях.
- Причина: ограничения проверки при редактировании относятся к отчёту о работе, а не к авторскому содержимому статьи. Сохранена отдельная короткая заметка о двух обозначениях сборки 906.
- Проверки: строгая сборка — `No issues found`; ссылки, изображения и якоря руководства проверены; служебные статусы отсутствуют; код не изменён; `git diff --check` прошёл.
- Визуальная проверка и выполнение на стенде не проводились; dev-сервер не перезапускался.

## 03.09.2026 22:57 — Уточнение версии сервера

- По подтверждению автора в обзоре и главе о группах указана версия `WebSoft HCM Server 2023.2.906`. Обе редакционные заметки о расхождении обозначений удалены; код не изменён.
- Проверки: строгая сборка — `No issues found`; проверены обозначения версии, отсутствие заметок, ссылки, изображения и якоря затронутых страниц; `git diff --check` прошёл.
- Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 03.09.2026 23:53 — Версии API тегов и уточнение примеров WebSocket

- По подтверждению автора теги отмечены как возможность сборки 906+: отдельная глава, меню, обзор и каталог. В группах явно разделены хранилище для 434 и теги для 906+. Особенности реализации ограничены Datex.XHTTP 1.24.4.27, без утверждения совместимости всех будущих версий.
- Abort выделен в общую главу closing.md, пример ограничен полным ID тестового соединения; сохранён переход со старого якоря tags/#abort. Указано отсутствие действия Abort у удалённого виртуального сокета.
- Исправлены описание json_compound и текстовые сообщения, добавлен пример EncodeJson; уточнены повторное подключение и force_remote. Адресный отбор использует полный ID, разбор группового ID проверяет сервис и структуру. Удалено неверное ограничение входящих сообщений только текстом; уточнены замена тегов, пустой JSON и числовые ID.
- Основание: resources/906/Datex.XHTTP.dll.decompiled — WebSocketContext.cs (GetWebSockets, приём, создание ID, очередь и отправка), DatexWebSocket.cs (методы тегов), DatexVirtualWebSocket.cs (Abort). Навык webtutor-platform использован для сохранения структуры SP-XML-примеров. Поведение libMain.main_socket не проверялось по DLL; исходники библиотеки 434 не исследовались.
- Проверки: строгая сборка — No issues found; 51 локальная ссылка, изображение и якорь проверены; кодовые блоки сбалансированы; все 23 изображения и HTML-клиент совпадают с индексом Git. Четыре модельных случая разбора ID прошли (не выполнение SP-XML); git diff --check прошёл.
- Визуальная проверка и выполнение на стенде не проводились. Dev-сервер и индекс Git не изменялись; resources и ранее подготовленные пользователем изменения сохранены.

## 03.09.2026 23:57 — Редактура руководства WebSocket

- Отредактированы семь страниц: инструкции приведены к единому обращению к читателю, убраны разговорные обороты и повторы, исправлены терминология и подписи. Сведения о версиях оформлены обычным текстом вместо избыточного блока опасности.
- Сохранены разделение сборок 434 и 906+, технические ограничения, ссылки и все примеры. Все блоки кода сравнены до и после правки и не изменились; изображения не редактировались.
- Причина: сделать руководство последовательным и удобным для чтения после технического уточнения.
- Проверки: строгая сборка — No issues found; проверены 51 локальная ссылка, изображение и якорь; git diff --check прошёл. Визуальная проверка и выполнение на стенде не проводились; dev-сервер и индекс Git не изменялись.

## 04.09.2026 00:22 — Уточнение сценария main_ws_service сборки 906

- Руководство уточнено по resources/906/wtv/wtv_main_ws_service.js, XML сервиса, libs/main_library.js и dotnetcore/core/linux/Datex.XHTTP.dll.decompiled. Добавлены условия доступа, формат команд, ответ с uid и разделение обычного текста и JSON-очереди штатного сервиса.
- В сборке 906 init_socket описан как замена тегов, close_socket отделён от закрытия соединения; пояснены перезапись служебных тегов, проверки прав call_method и ограничения ответа об ошибках. Примеры отправки группе используют EncodeJson с ExportLargeIntegersAsStrings и json_compound=true; отличия формата от снимков экрана обозначены рядом.
- HTML-клиент использует heartbeat $hbt/$ok вместо JSON ping/pong; автоматическая проверка выключена по умолчанию. Описан отдельный путь XML при копировании сервиса. Навык webtutor-platform применён для сверки примеров с локальной реализацией; описание сборки 434 не переписывалось по исходникам 906.
- Проверки: строгая сборка — No issues found; 52 локальные ссылки, изображения и якоря; пять JSON-примеров разобраны; node --check прошёл. Тест с имитацией DOM/WebSocket проверил ручной heartbeat, ответ $ok, таймер, показ массива JSON, отсутствие ответа на чужой ping, ручную отправку и остановку таймера при отключении. git diff --check прошёл.
- Визуальная проверка и выполнение на WebTutor не проводились. Исходники resources, изображения, dev-сервер и индекс Git не изменялись.

## 04.09.2026 00:58 — Сверка руководства со сборкой 434

- По исходникам resources/434 общими для 434 и 906 описаны JSON-очередь, heartbeat, ответ с uid, проверка прав call_method и ограничения передачи ошибок. Неясная фраза об ответе сервиса заменена последовательностью: команда клиента, обработка, ответ в то же соединение, общий режим очереди.
- Групповой пример 434 использует сериализованный JSON с json_compound=true. Уточнены отсутствие дубликатов подписчика, участие в нескольких группах, отличие регистрации от активности, зависимость close_socket от внешнего context и отсутствие автоматической отписки в обработчике закрытия. Различия упаковки готовых JSON-массивов и сигнатур GetWebSockets сохранены по версиям.
- Обновлены подписи heartbeat клиента и общая глава о закрытии; исходники ресурсов и изображения не изменялись. Навык webtutor-platform применён для сохранения различий SP-XML и API сборок.
- Проверки: строгая сборка — No issues found; 54 локальные ссылки, изображения и якоря проверены; шесть JSON-примеров разобраны; node --check для клиента и git diff --check прошли. Проверено использование JSON в групповом примере 434.
- Визуальная проверка и выполнение на стенде не проводились; dev-сервер и индекс Git не изменялись.

## 07.09.2026 10:55 — Адрес локального WebSocket-стенда

- Во всех примерах руководства и в значении поля тестового клиента адрес `main_ws_service` заменён на `ws://localhost:80/services/main_ws_service` с явным портом 80.
- Причина: привести инструкцию к адресу, используемому при испытаниях сборки 434, и исключить расхождения между главами и клиентом. Единичный отказ без явного порта не описан как требование платформы, поскольку повторить его не удалось.
- Проверки: строгая сборка — `No issues found`; старые адреса `domain.ru` для `main_ws_service` отсутствуют; `git diff --check` прошёл. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 13:26 — Проверка heartbeat по DLL сборок 434 и 906

- Для сборки 434 указана фактическая версия Datex.XHTTP `1.22.6.9` из образа `WebSoft HCM Server 2022.1.3.434`; прежнее обозначение `1.22.10.29` удалено.
- Проверка `$hbt` / `$ok` ограничена сборкой 906: в Datex.XHTTP `1.24.4.27` сообщение перехватывается до сервиса, а в `1.22.6.9` передаётся в `main_ws_service` и вызывает ошибку `ParseJson()`.
- Причина: привести руководство и тестовый клиент в соответствие с декомпилированными DLL из образов и результатом испытания сборки 434.
- Проверки: строгая сборка — `No issues found`; проверено 47 локальных ссылок и вложений; `node --check` для встроенного клиента и `git diff --check` прошли. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 14:08 — Разделение руководства WebSocket по сборкам

- Созданы самостоятельные разделы для WebSoft HCM Server `2022.1.3.434` и `2023.2.906`: в каждом есть обзор, подключение, отправка сообщений, группы и закрытие; работа с тегами находится только в разделе сборки 906.
- Примеры приведены к API своей DLL: `GetWebSockets()` без параметров и хранилище подписчиков для 434; фильтр, `force_remote`, теги и heartbeat для 906. Общими оставлены обзор выбора версии, изображения и тестовый HTML-клиент.
- Прежние URL глав сохранены как страницы выбора версии. Меню и каталог руководств обновлены; раздел сборки 906 подготовлен для последующих явно отмеченных уточнений сборки 1515.
- Причина: исключить смешение несовместимых инструкций и упростить последовательное прохождение материала для конкретной сборки.
- Проверки: строгая сборка — `No issues found`; проверены 18 страниц, 82 локальные ссылки и вложения, баланс блоков кода и синтаксис JavaScript клиента; `git diff --check` прошёл.
- Визуальная проверка и повторное выполнение примеров на стендах не проводились; dev-сервер не перезапускался.

## 07.09.2026 15:24 — Терминология разделов по сборкам

- В пользовательских материалах слово «ветка» заменено на «сборка» или «раздел сборки», когда речь идёт о версии WebSoft HCM Server.
- Конструкции `call_method` и `close_socket` названы обработчиками.
- Причина: не смешивать структуру документации, версии сервера и ветвление программного кода.
- Проверки: строгая сборка — `No issues found`; проверены 82 локальные ссылки и вложения; `git diff --check` прошёл. В опубликованных страницах слово «ветка» отсутствует. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 15:38 — Имена файлов для новых скриншотов

- Перед каждым из 37 изображений в разделах сборок 434 и 906 добавлено имя файла для нового скриншота. Имена уникальны и содержат номер сборки, например `434-first-connect.png` и `906-first-connect.png`.
- В обзорах обеих сборок указана папка сохранения `docs/guides/websockets/img/`. Текущие изображения и ссылки на них не изменены: новые файлы можно подготавливать постепенно.
- Причина: имя будущего файла видно непосредственно рядом со сценарием, поэтому снимок можно сразу сохранить без отдельного списка соответствий.
- Проверки: строгая сборка — `No issues found`; для всех 37 изображений найдена расположенная перед ним подпись, все имена уникальны; проверены 87 локальных ссылок и вложений; `git diff --check` прошёл. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 15:57 — Последовательность главы об отправке сообщений 434

- Первый опыт теперь явно использует подключение без `X-StatefulSocketId`: сборка 434 автоматически создаёт ID вида `/services/main_ws_service-v-<GUID>`. Скриншот полученного текстового сообщения перенесён непосредственно под ожидаемый результат и получил имя `434-first-server-message.png`.
- Раздел «Объединение JSON» переименован в «Режим очереди `json_compound`». Сначала объяснена упаковка серверных сообщений при `false` и `true`, затем отдельно указано, почему предварительная команда `init_socket` влияет на режим уже созданной очереди. Направление передачи во всех примерах обозначено как сервер → клиент.
- Основание: `resources/434/.../WebSocketContext.cs` — автоматическое формирование ID в `ProcessWebSocketRequestAsync`, фиксация `json_compound` при создании очереди и упаковка элементов в `ControlWebSocketsQueue`. Навык webtutor-platform использован для сохранения совместимого SP-XML-примера и отделения подтверждённого поведения DLL от сценария сервиса.
- Добавлены восемь ранее пропущенных имён файлов у изображений внутри предупреждений разделов 434 и 906; предыдущая запись исправлена до фактических 37 изображений. Сами изображения и ссылки не изменены.
- Проверки: строгая сборка — `No issues found`; все 37 изображений имеют расположенные перед ними уникальные имена файлов; проверены 87 локальных ссылок и вложений; `git diff --check` прошёл. Визуальная проверка и выполнение обновлённой последовательности на стенде не проводились; dev-сервер не перезапускался.

## 07.09.2026 16:38 — Создание копии WebSocket-сервиса для сборки 434

- В главу подготовки добавлены шаги копирования пары файлов `wtv_main_ws_service`, изменения `WEB-SERVICE-PATH` и подключения второго тестового клиента к `rtk_ws_service`.
- Из предупреждения о массовой рассылке добавлена прямая ссылка на эту инструкцию. Причина: эксперимент с двумя сервисами ранее требовал действий, которые не были описаны полностью.
- Проверки: строгая сборка и проверка ссылки выполнены; визуальная проверка и выполнение инструкции на стенде WebTutor не проводились, dev-сервер не запускался.

## 07.09.2026 17:47 — Регистрация копии WebSocket-сервиса через api_ext

- Инструкция для сборки 434 исправлена по рабочему сценарию стенда: пара `rtk_ws_service.xml` и `.js` размещается в `source`, а XML регистрируется через `source/api_ext.xml`; сохранён `CODE-LIB="1"` для автоматической загрузки соседнего JS-файла.
- Добавлены полный фрагмент `api_ext.xml`, обязательный перезапуск WebTutor и необязательное исправление диагностической подписи в JS. Прежняя инструкция о размещении копии непосредственно в `wtv` удалена.
- Основание: `wtv_view_main.xml` сборки 434 в `OnAppInit` читает `x-local://source/api_ext.xml` и выполняет `FetchDoc(...).SetShared()` для перечисленных библиотек. Навык webtutor-platform использован для сверки разметки и механизма загрузки.
- Проверки: строгая сборка выполнена; локальная ссылка на раздел создания копии проверена. Визуальная проверка не выполнялась; dev-сервер не перезапускался. Рабочий сценарий подтверждён пользователем на стенде WebTutor 434.

## 07.09.2026 18:03 — Запуск агента для вывода socketId

- В главе об отправке сообщений сборки 434 явно указано, что код вывода идентификаторов нужно поместить в агент и запустить. Причина: прежняя формулировка описывала результат, но пропускала действие читателя.
- Проверки: строгая сборка выполнена; `git diff --check` прошёл. Код примера не изменён. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 18:19 — Уточнение о тегах WebSocket

- Во вводном абзаце главы о группах указано, что в сборке 434 теги соединений ещё не поддерживаются, а в более поздней сборке 906 уже доступны. Добавлена ссылка на главу о тегах в сборке 906. Причина: фраза «без тегов» не объясняла, что это ограничение версии.
- Проверки: строгая сборка и `git diff --check`. Код примеров не изменён. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 18:27 — Формулировка о группировке клиентов

- Из главы о группах сборки 434 убрано преждевременное упоминание тегов и ссылка на сборку 906. Вместо неясного термина «соединения» указано, что пример группирует WebSocket-клиентов через `X-StatefulSocketId`.
- Проверки: строгая сборка и `git diff --check`. Код примеров не изменён. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 18:35 — Объявления переменных в цикле SP-XML Script

- В групповом примере сборки 434 объявления `idParts`, `data` и `params_str` вынесены из тела цикла. Все переменные блока явно объявлены до цикла. Причина: SP-XML Script повторно выполнял `var` на каждой итерации и завершал агент с ошибкой `Variable idParts already declared`.
- Проверки: в телах циклов главы больше нет объявлений `var`; строгая сборка и `git diff --check`. Исправленный пример на стенде не запускался. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 19:05 — Проверка состава WebSocket-группы

- После открытия второго соединения добавлено явное действие: повторно запустить агент с `get_user_data` и убедиться, что `socket_id` нового клиента не появился в `result`. Причина: прежний текст не связывал открытие соединения с наблюдаемой проверкой хранилища.
- Проверки: строгая сборка и `git diff --check`. Код примеров не изменён. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 19:11 — Опыт с `close_socket` в сборке 434

- В раздел отписки добавлена команда `close_socket` для группы `ws_group` и пошаговая проверка результа через `get_user_data`. При сохранении записи указано проверить журнал сервера.
- Основание: `resources/434/wtv/libs/main_library.js` — ветка `close_socket` фильтрует список по `context.WebSocketCurrentId`, хотя функция `main_socket` получает ID как `sWebsocketID`. Поэтому исход опыта заранее не утверждается.
- Проверки: строгая сборка и `git diff --check`. Опыт на стенде ещё не выполнен. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 19:19 — Результат `close_socket` на стенде 434

- По результату опыта `close_socket` обозначен как неработающий в штатной реализации сборки 434. В руководство добавлены фактические ошибки `main_socketcontext not defined` и `Unknown object property: socket_object`.
- Первая ошибка связана с обращением к необъявленному `context`; вторая возникает при попытке сервиса отправить несформированный `socket_object`. Запись в `ws_group_recipients` не удаляется.
- Проверки: поведение подтверждено пользователем на стенде WebTutor 434; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 19:34 — Синхронизация главы о закрытии WebSocket 434

- В главе о закрытии убрано предположение о зависимости `close_socket` от внешнего контекста. Вместо него указан подтверждённый результат: штатная реализация сборки 434 не работает из-за обращения к необъявленному `context`.
- Ссылка ведёт прямо к разделу с командой и фактическими ошибками. Причина: устранить противоречие между главами о группах и закрытии.
- Проверки: строгая сборка, проверка ссылки на якорь и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 19:56 — Поведение `Abort()` на стенде 434

- В главе о закрытии убрано утверждение, что `Abort()` закрывает локальное соединение. Вызов через SP-XML приведён к явной форме `CallObjectMethod`.
- Зафиксирован результат стенда: локальный `ManagedWebSocket` остался в `GetWebSockets()`, браузер не получил событие закрытия, последующее сообщение не доставлено, а `close(context)` не вызван. Поэтому `Abort()` обозначен как ненадёжный способ закрытия в сборке 434.
- Основание: декомпилированные `WebSocketContext.cs` и `VirtualWebSocket.cs` из Datex.XHTTP `1.22.6.9`, а также результат опыта пользователя на WebTutor 434.
- Проверки: строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 23:38 — Обновлённые скриншоты раздела 434

- Из страниц подключения, отправки сообщений и групп сборки 434 удалены 18 временных подсказок с именами будущих файлов. На этом этапе ссылки оставались на прежние изображения в `img/1`–`img/5`.
- Удалена устаревшая редакторская оговорка о расхождении формата групповых сообщений со снимками. Раздел сборки 906 не изменён: подсказки для его будущих скриншотов сохранены.
- Проверки: все 18 изображений раздела 434 существуют и распознаются как PNG; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 07.09.2026 23:42 — Подключение новых скриншотов сборки 434

- Все 18 PNG из пользовательской папки `img/434-test` скопированы в публикуемый каталог `docs/guides/websockets/img/434/`. Исходная папка сохранена.
- Страницы подключения, отправки сообщений и групп сборки 434 переключены на новые файлы. Старые изображения в `img/1`–`img/5` не заменялись, поскольку их продолжает использовать раздел сборки 906.
- Проверки: исходные и опубликованные файлы попарно совпадают; все 18 ссылок раздела 434 ведут в `img/434/`; ссылок на прежние изображения и технических подсказок в разделе 434 не осталось; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 00:08 — Имена новых скриншотов сборки 906

- Для тестирования сборки 906 подтверждены 19 уникальных имён скриншотов с префиксом `906-`; имя указано непосредственно перед каждым текущим изображением.
- В обзоре раздела местом сохранения указана папка `img/906-test/` в корне проекта, по аналогии с завершённой подготовкой скриншотов 434. Папка создана; старые иллюстрации и ссылки пока не изменялись.
- Проверки: число имён совпадает с числом изображений, повторяющихся имён нет; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 00:40 — Подготовка руководства 906 к стендовому тестированию

- Структура страниц подключения, отправки сообщений, групп и закрытия сборки 906 сверена с исправленным сценарием 434. Добавлены полная регистрация копии сервиса через `source/api_ext.xml`, ссылка на неё из массовой рассылки, явное создание и запуск агентов, новое соединение без `X-StatefulSocketId` для первого текстового сообщения и последовательное объяснение режима `json_compound`.
- Снимок текстового сообщения перенесён к первому опыту. Пример отправки JSON дополнен созданием сборки и полным `socketId`. Во всех циклах переменные объявлены до запуска; убраны объявления `var` внутри циклов, исправлены пропущенные точки с запятой и опечатки в термине «тег».
- В главе групп для опытов с тегами явно задан отдельный ID `777`; вывод и изменение тегов ограничены целевым сервисом или полным ID. Глава тегов теперь изменяет только `/services/main_ws_service-s-777`, а не все найденные соединения.
- Сценарий `Abort()` разделён на три отдельных запуска: контрольная отправка, вызов метода и проверка `GetWebSockets()` с повторной отправкой. Успешное закрытие не утверждается до проверки на стенде 906. Для `close_socket` указано подтверждённое исходниками отличие: ветка `main_socket` пуста.
- Основание: `resources/906`, `wtv_view_main.xml` из образа `hcm_2023.2.906` и подготовленные файлы стенда `906_PSQL`. Механизм `api_ext.xml`, реализация тегов и различия локального и виртуального `Abort()` сверены с исходниками и декомпилированной Datex.XHTTP `1.24.4.27`.
- Проверки: строгая сборка, локальные ссылки и якоря, соответствие 19 имён 19 изображениям, отсутствие `var` внутри циклов и `git diff --check`. Изменённые примеры на стенде 906 ещё не выполнялись. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 11:25 — Скриншот проверки связи в сборке 906

- После описания обмена `$hbt` / `$ok` добавлено имя будущего скриншота `906-check-connect-hbt.png`. Файл следует сохранить в `img/906-test/`.
- Ссылка на изображение пока не добавлена, поскольку PNG ещё отсутствует; это сохраняет строгую сборку без битых вложений.
- Проверки: имя уникально; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 11:46 — Отдельное соединение для `json_compound = true`

- В сценарий отправки сообщений 906 добавлено отдельное подключение с `X-StatefulSocketId=json-compound-true`. Агент использует соответствующий полный ID `/services/main_ws_service-s-json-compound-true` вместо ранее необъяснённого `-s-123`.
- Устранено расхождение между текстом и кодом: агент отправляет сериализованный объект `{"text":"Привет"}`, а ожидаемый результат явно показан как массив `[{"text":"Привет"}]`. Первое GUID-соединение остаётся примером текстовой очереди с `json_compound = false`.
- Причина: режим очереди фиксируется при её создании, поэтому сравнение `false` и `true` должно выполняться на разных соединениях.
- Проверки: строгая сборка и `git diff --check`. Сценарий на стенде 906 ещё не выполнен. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 12:04 — Единый агент для сравнения режимов очереди 906

- Адресный опыт с `X-StatefulSocketId=json-compound-true` заменён последовательным сравнением на двух соединениях без `X-StatefulSocketId`. В агент первого опыта добавлена переменная `jsonCompound`: первый запуск выполняется с `false`, второй — с `true`.
- Один агент показывает фиксацию режима очереди: при втором запуске первая уже существующая очередь остаётся текстовой, а для нового второго соединения создаётся очередь `json_compound = true`. Команда `init_socket` для этого опыта не требуется.
- Отдельно указано, что упаковка произвольного текста в квадратные скобки не образует корректный JSON и для прикладных данных нужен `EncodeJson`.
- Блок массовой рассылки продолжает тот же сценарий: предыдущие клиенты закрываются, создаются новые подключения к `main_ws_service` и `rtk_ws_service`, после чего тот же агент запускается с `jsonCompound = true`. Обе новые очереди создаются в режиме `true`.
- Причина: прежний сценарий вводил необъяснённый адресный ID и разрывал связь между первым агентом, сравнением режимов и последующей массовой рассылкой.
- Проверки: строгая сборка и `git diff --check`. Обновлённая последовательность на стенде 906 ещё не выполнена. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 12:25 — Создание соединения с `X-StatefulSocketId=123`

- В разделе адресной отправки строка подключения превращена в явный шаг: закрыть клиенты предыдущего опыта и открыть новое соединение с `X-StatefulSocketId=123`.
- Перед следующим агентом указано оставить это соединение открытым и добавить второе без `X-StatefulSocketId`. Причина: прежний URL появлялся без действия и нарушал последовательность руководства.
- Проверки: строгая сборка и `git diff --check`. Сценарий на стенде 906 ещё не выполнен. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 13:16 — Разделение группы и произвольных тегов в сборке 906

- Раздел фильтрации переименован в «Отправка группе через `socket_type`». Пояснено различие версий: в 434 поле `socket_type` задаёт ключ хранилища подписчиков, а в 906 `main_socket` записывает его в одноимённый тег соединения.
- Для клиентов `111` и `222` добавлены разные произвольные теги `role=speaker` и `role=listener` при общем `socket_type=ws_group`. Первый агент с фильтром `$socket_type:ws_group` отправляет сообщение группе из двух клиентов.
- Добавлен отдельный агент с фильтром `$role:speaker`, который выбирает только клиент `111`. Для будущих результатов добавлены имена `906-send-message-tag-filter-match.png` и `906-send-message-tag-filter-no-match.png`.
- Основание: `resources/906/wtv/libs/main_library.js` присваивает `oTag.socket_type`, а затем добавляет свойства из `socket_tags`; Datex.XHTTP `1.24.4.27` обрабатывает фильтр вида `$<имя>:<значение>` по коллекции `Tags`.
- Проверки: строгая сборка, уникальность имён скриншотов, отсутствие `var` внутри циклов и `git diff --check`. Новые фильтры на стенде 906 ещё не выполнены. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 13:29 — Последовательное объяснение тегов `init_socket`

- Начало раздела перестроено вокруг фактического преобразования команды: поле `socket_type` копируется в одноимённый тег, служебные теги `user_id`, `socket_type` и `pong` создаются до обработки массива `socket_tags`, а произвольные значения могут их перезаписать.
- После команды `init_socket` добавлена таблица итогового набора тегов с указанием источника каждого значения. Объяснение полной замены тегов при повторном вызове и предупреждение о недоверенных клиентских значениях перенесены до практики.
- Практическая часть выделена в раздел «Отправка по тегам»: сначала поиск `socket_type=ws_group` обходом коллекции `Tags`, затем тот же отбор встроенным фильтром `GetWebSockets`, после него — фильтр по произвольному тегу `role=speaker`.
- Удалено повторное объяснение `socket_type` перед фильтром. Причина: прежняя последовательность называла поле тегом до объяснения механизма и смешивала служебные и произвольные теги.
- Проверки: строгая сборка, структура заголовков, уникальность 22 подготовленных имён скриншотов, отсутствие `var` внутри циклов и `git diff --check`. Изменённая последовательность на стенде 906 ещё не выполнена. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 15:30 — Пояснение JSON при отправке по тегам

- Неясная фраза «отправляйте JSON, как и сам сервис» заменена явной причинно-следственной связью: после `init_socket` очередь работает с `json_compound = true`, поэтому агент сериализует объект с полем `text`, а не передаёт обычную строку.
- Упоминание сериализации больших целых удалено из вводного абзаца как не относящееся к текущему сообщению.
- Проверки: строгая сборка и `git diff --check`. Код примера не изменён. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 15:42 — Целевой клиент в примере отправки по тегам

- Неопределённое выражение «очередь соединения» заменено ссылкой на конкретный клиент `/services/main_ws_service-s-777`, который в предыдущем шаге отправил `init_socket`.
- Явно указано, что очередь `json_compound = true` создал ответ сервиса этому клиенту и поэтому следующее сообщение для него сериализуется в JSON.
- Проверки: строгая сборка и `git diff --check`. Код примера не изменён. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 15:52 — Три клиента для опытов с тегами 906

- Соединение `777` оставлено только в разделе назначения и просмотра тегов. Перед отправкой по тегам добавлены отдельные клиенты `111`, `222` и `333`.
- Клиенты получили различающиеся наборы: `111` — `socket_type=send_group`, `is_person=1`; `222` — `socket_type=send_group`, `is_person=0`; `333` — `socket_type=other_group`, `is_person=1`. Значения сведены в таблицу перед опытами.
- Одна тройка используется для трёх способов: ручной поиск `socket_type` в цикле выбирает `111` и `222`; фильтр `$socket_type:send_group` возвращает ту же пару; фильтр произвольного тега `$is_person:1` выбирает `111` и `333`.
- Для фиксации результатов подготовлены дополнительные имена скриншотов второго совпадения и отсутствия сообщения. Причина: каждый способ отбора должен проверяться на нескольких клиентах с явно различающимися тегами.
- Проверки: строгая сборка, уникальность 25 подготовленных имён скриншотов, отсутствие `var` внутри циклов и `git diff --check`. Обновлённые опыты на стенде 906 ещё не выполнены. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 16:19 — Возможности фильтра `GetWebSockets`

- В главу о группах добавлена таблица сокращённых фильтров: без отбора, наличие тега, одно значение и несколько значений одного тега. Для текущих клиентов приведён агент с `$socket_type:[send_group,other_group]`, который по реализации DLL должен выбрать `111`, `222` и `333`.
- Указаны ограничения: список значений работает как «ИЛИ» только для одного имени тега; разные теги требуют дополнительной проверки `Tags`; пробелы после запятых не удаляются; сравнение учитывает регистр; разделители не экранируются; сервис нужно ограничивать отдельно; `force_remote` не связан с включением фильтра.
- Добавлено имя будущего скриншота `906-get-websockets-multiple-values.png`. Основание: разбор `GetWebSockets(string filter_lambda, bool force_remote)` в Datex.XHTTP `1.24.4.27`.
- Проверки: строгая сборка, уникальность 26 подготовленных имён скриншотов, отсутствие `var` внутри циклов и `git diff --check`. Фильтр со списком значений на стенде 906 ещё не выполнен. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 16:24 — Самостоятельная подготовка главы о тегах 906

- В `tags.md` добавлены собственные шаги подготовки: подключение `X-StatefulSocketId=tags-test`, команда `init_socket` с начальными тегами `webinar_id` и `role`, а также таблица полного ожидаемого набора служебных и произвольных тегов.
- Все агенты главы переключены с соединения `777` из предыдущей главы на полный ID `/services/main_ws_service-s-tags-test`. Указано выполнять примеры сверху вниз, поскольку `RemoveTag`, `SetTagsFromJson` и `AddOrUpdateTag` последовательно меняют состояние.
- Причина: глава должна быть выполнима отдельно и не зависеть от прохождения раздела о группах.
- Проверки: строгая сборка, локальная ссылка на подготовку, отсутствие прежнего целевого ID в `tags.md`, отсутствие `var` внутри циклов и `git diff --check`. Новая последовательность на стенде 906 ещё не выполнена. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 16:57 — Результат `Abort()` на стенде 906

- В главе о закрытии зафиксирован подтверждённый результат: локальный `Abort()` закрыл клиент с сообщением `code=1006, reason=`, удалил ID `/services/main_ws_service-s-555` из `GetWebSockets()`, а сообщение после прерывания не было доставлено.
- Пояснено, что `1006` обозначает аварийное завершение без кадра закрытия и `Abort()` не заменяет согласованное закрытие с кодом `1000`. Добавлено явное отличие от стенда 434, где ID оставался в `GetWebSockets()`, а браузер сохранял активное состояние.
- Основание: результат пользователя на WebTutor 906 и журналы стенда `906_PSQL`; вызовы агента и последующее отсутствие ID подтверждены в `xhttp_2026-09-08.log`.
- Несуществующие ссылки на `906-pref-abort.png` и `906-abort.png` заменены именами будущих скриншотов; после появления файлов в `img/906-test/` их можно подключить к странице.
- Проверки: строгая сборка, отсутствие битых ссылок на два будущих изображения и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 17:11 — Подключение итоговых скриншотов и редактура раздела 906

- Все 27 PNG из `docs/guides/websockets/img/906/` подключены к соответствующим местам страниц подготовки, отправки сообщений, групп и закрытия. Старые ссылки на `img/1`–`img/6`, строки с именами будущих файлов и раздел с инструкцией по подготовке скриншотов удалены.
- Для новых сравнительных снимков добавлены содержательные подписи клиентов `111`, `222` и `333`. Две отсутствовавшие ранее иллюстрации опыта `Abort()` подключены к подтверждённым шагам.
- При редактуре исправлены `так же` → `также`, двойной пробел в сообщении с `socketId`, повтор вводного описания Datex.XHTTP и необязательный альтернативный комментарий внутри агента поиска тегов.
- Проверки: каждый из 27 PNG корректно распознаётся, имеет уникальное содержимое и используется ровно в одном файле раздела 906; старых путей и редакторских подсказок не осталось; синтаксически проверены 15 JavaScript-блоков и разобраны 7 JSON-блоков; строгая сборка, локальные ссылки и `git diff --check`. Браузерная визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 17:34 — Удаление устаревших изображений WebSocket

- После перехода разделов на `img/434/` и `img/906/` удалены каталоги `docs/guides/websockets/img/1`–`img/6` с 23 устаревшими PNG. Перед удалением подтверждено отсутствие ссылок на них во всех страницах статьи.
- Актуальные 18 изображений сборки 434 и 27 изображений сборки 906 сохранены. Исходники `resources/434`, `resources/906` и рабочие папки вне `docs/` не изменялись.
- Проверки: все ссылки на изображения разрешаются в существующие файлы; неиспользуемых PNG в публикуемом каталоге статьи не осталось; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 17:41 — Удаление рабочего каталога `img`

- Удалён корневой каталог `img`: 18 резервных PNG из `img/434-test` и пустая папка `img/906-test`. Перед удалением все файлы 434 попарно сравнены с опубликованными копиями в `docs/guides/websockets/img/434/`; различий нет.
- Актуальные изображения статьи в `docs/guides/websockets/img/434/` и `docs/guides/websockets/img/906/` не изменялись.
- Проверки: корневой `img` отсутствует; строгая сборка и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.

## 08.09.2026 18:15 — Удаление страниц-переходников WebSocket

- Удалены верхнеуровневые `docs/guides/websockets/connection.md`, `messages.md`, `groups.md`, `tags.md` и `closing.md`. Совместимость с прежними URL больше не поддерживается.
- Сохранены общая страница выбора сборки `index.md`, тестовый клиент `ws-client.md` и самостоятельные руководства в каталогах `434/` и `906/`.
- Перед удалением подтверждено отсутствие входящих ссылок и пунктов навигации на переходники.
- Проверки: строгая сборка, локальные ссылки и `git diff --check`. Визуальная проверка не выполнялась; dev-сервер не перезапускался.
